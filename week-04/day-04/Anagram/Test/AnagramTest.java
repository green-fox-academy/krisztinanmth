import org.junit.Assert;
import org.junit.Test;

import static org.junit.Assert.*;

public class AnagramTest {

  @Test
  public void testingIsAnagramMethod() {
    isAnagram anagram = new isAnagram();
    Assert.assertEquals(false, );
  }
}